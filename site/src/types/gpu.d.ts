export interface Gpu {
  _id: string;
  name: string;
  price: number;
  chipset: string;
  memory: number;
  core_clock: number;
  boost_clock: number;
  color: string;
  length: number;
  benchmark: number;
}
